ci:
  autoupdate_schedule: quarterly

repos:
  # Meta hooks
  - repo: meta
    hooks:
      - id: check-useless-excludes
      - id: check-hooks-apply

  # General file checks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-merge-conflict
      - id: check-yaml
      - id: destroyed-symlinks
      - id: detect-private-key
      - id: end-of-file-fixer
      - id: fix-byte-order-marker
      - id: mixed-line-ending
      - id: trailing-whitespace

  # Shell script linting
  - repo: https://github.com/shellcheck-py/shellcheck-py
    rev: v0.11.0.1
    hooks:
      - id: shellcheck
        args: ["--severity=warning"]

  # SCSS/CSS linting and formatting
  - repo: https://github.com/pre-commit/mirrors-prettier
    rev: v4.0.0-alpha.8
    hooks:
      - id: prettier
        types_or: [scss, css, html, yaml, markdown, json]

  # HTML linting
  - repo: https://github.com/rtibbles/pre-commit-hooks
    rev: v2.0.1
    hooks:
      - id: html-lint
        args: ["--disable=optional_tags,protocol"]

  # Ruby linting and formatting (if Ruby files are present)
  - repo: https://github.com/rubocop/rubocop
    rev: v1.88.1
    hooks:
      - id: rubocop
        args: ["--auto-correct"]
